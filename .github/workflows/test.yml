name: Test Self-Hosted Runner

on: 
  push:   # 当你 push 代码到仓库时触发

jobs:
  build:
    runs-on: self-hosted   # 使用你刚配置的 self-hosted runner
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Run a simple command
        run: echo "Hello from self-hosted runner on $HOSTNAME"
